/**
 * Model class representing the raw beacon region received from the native event emitter.
 */
export default class RawRegionData {
    /**
     * The UUID of the beacon (region).
     */
    public uuid: string | undefined;
}
