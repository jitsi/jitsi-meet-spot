/**
 * Action type to signal that the app has successfully mounted.
 */
export const APP_MOUNTED = 'APP_MOUNTED';

/**
 * Action type to signal that the app will be unmounted.
 */
export const APP_WILL_UNMOUNT = 'APP_WILL_UNMOUNT';
