export default {
    warn(...args: any[]) {
        // eslint-disable-next-line no-console
        console.warn(...args);
    },

    error(...args: any[]) {
        // eslint-disable-next-line no-console
        console.error(...args);
    }
};
